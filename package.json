{
  "name": "section7",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "python -m unittest discover -p test_*.py",
    "install": "pip install -r requirements.txt",
    "start": "python run.py",
    "sonar": "sonar-scanner",
    "coverage": "coverage run --source . -m unittest discover && coverage xml -o coverage-reports/coverage.xml"
  },
  "author": "",
  "license": "ISC"
}